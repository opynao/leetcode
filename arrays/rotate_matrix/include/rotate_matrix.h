#pragma once
#include <vector>

void rotate1(std::vector<std::vector<int>> &);
void rotate2(std::vector<std::vector<int>> &);

void helper_transpose(std::vector<std::vector<int>> &, int N);
void helper_reverse(std::vector<int> &, int N);
